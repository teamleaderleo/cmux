name: Fieldwork PTY event delivery queue

on:
  push:
    branches:
      - fieldwork/nonlinear-resource-collapse
    paths:
      - .github/workflows/fieldwork-pty-event-delivery.yml
      - scripts/fieldwork/cmux-resource-collapse/pty_event_delivery_probe.patch
  workflow_dispatch:

permissions:
  contents: read

jobs:
  pty-event-delivery:
    runs-on: macos-15
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - name: Toolchain and revision
        run: |
          swift --version
          git rev-parse HEAD
      - name: Apply measurement-only PTY delivery probe
        run: |
          git apply --check --recount scripts/fieldwork/cmux-resource-collapse/pty_event_delivery_probe.patch
          git apply --recount scripts/fieldwork/cmux-resource-collapse/pty_event_delivery_probe.patch
          git diff --check
      - name: Build probe
        env:
          CMUX_FIELDWORK_PTY_DELIVERY: "1"
          CMUX_FIELDWORK_PTY_DELIVERY_TOTAL_BYTES: "1048576"
          CMUX_FIELDWORK_PTY_DELIVERY_PAYLOAD_BYTES: "32768"
          CMUX_FIELDWORK_PTY_DELIVERY_BLOCKED: "0"
        run: |
          swift test \
            --package-path Packages/macOS/CmuxRemoteDaemon \
            --filter RemoteDaemonRPCClientPTYDeliveryQueueProbeTests
      - name: Healthy delivery scaling
        env:
          CMUX_FIELDWORK_PTY_DELIVERY: "1"
          CMUX_FIELDWORK_PTY_DELIVERY_PAYLOAD_BYTES: "32768"
          CMUX_FIELDWORK_PTY_DELIVERY_BLOCKED: "0"
        run: |
          mkdir -p pty-event-delivery-results
          for mib in 1 10 50 200; do
            bytes=$((mib * 1024 * 1024))
            echo "=== healthy mib=${mib} ===" | tee -a pty-event-delivery-results/healthy.log
            CMUX_FIELDWORK_PTY_DELIVERY_TOTAL_BYTES="$bytes" \
              swift test \
                --skip-build \
                --package-path Packages/macOS/CmuxRemoteDaemon \
                --filter RemoteDaemonRPCClientPTYDeliveryQueueProbeTests \
              2>&1 | tee -a pty-event-delivery-results/healthy.log
          done
      - name: Blocked delivery scaling
        env:
          CMUX_FIELDWORK_PTY_DELIVERY: "1"
          CMUX_FIELDWORK_PTY_DELIVERY_PAYLOAD_BYTES: "32768"
          CMUX_FIELDWORK_PTY_DELIVERY_BLOCKED: "1"
        run: |
          for mib in 1 10 50 200; do
            bytes=$((mib * 1024 * 1024))
            echo "=== blocked mib=${mib} ===" | tee -a pty-event-delivery-results/blocked.log
            CMUX_FIELDWORK_PTY_DELIVERY_TOTAL_BYTES="$bytes" \
              swift test \
                --skip-build \
                --package-path Packages/macOS/CmuxRemoteDaemon \
                --filter RemoteDaemonRPCClientPTYDeliveryQueueProbeTests \
              2>&1 | tee -a pty-event-delivery-results/blocked.log
          done
      - name: Preserve PTY delivery measurements
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pty-event-delivery-results
          path: pty-event-delivery-results/*.log
